apiVersion: v1
kind: Service
metadata:
  name: myapp-service
spec:
  type: NodePort
  ports:
    - targetPort: 80 # if missing same as port
      port: 80  # only this is mandatory
      nodePort: 30008 # if missing random port 
  selector:
    app: myapp  # labels of the pod that needs to be connected
